#include "../TowerDefence/GameObject/DynamicObject/PlayerFigure.hpp"
#include "catch_files/catch.hpp"

//std::string MEDIADIR = "../TowerDefence/media_files/png_images/";

TEST_CASE("updateObject() för att hålla in w")
{
  // sf::Texture texture;
  // PlayerFigure p_f(0, 0, &texture, std::sqrt(2));
  // sf::Vector2f coordinates (0, 0);
  // sf::Vector2f coordinates_2 (-1, -1);
  // CHECK(p_f.getPosition() == coordinates);
  // p_f.updateObject(); // håll in w och a!!!
  // CHECK(p_f.getPosition() == coordinates_2);
}